{
  "name": "testing-project",
  "version": "1.0.1",
  "description": "Automated testing pipeline with improved dependencies and fixes",
  "main": "index.js",
  "scripts": {
    "test:unit": "jest --coverage --testResultsProcessor jest-junit",
    "test:integration": "mocha tests/integration --reporter mocha-junit-reporter || echo 'No integration tests found'",
    "test:api": "jest --coverage --testResultsProcessor jest-junit",
    "test:e2e": "xvfb-run --auto-servernum cypress run --reporter junit --reporter-options 'mochaFile=results/e2e-results.xml'",
    "lint": "eslint ."
  },
  "engines": {
    "node": ">=20.9.0"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "mocha": "^11.1.0",
    "cypress": "^13.17.0",
    "supertest": "^6.3.0",
    "mocha-junit-reporter": "^2.0.2",
    "jest-junit": "^16.0.0",
    "eslint": "^8.57.1",
    "xvfb-maybe": "^0.2.1"
  },
  "jest": {
    "collectCoverage": true,
    "coverageDirectory": "coverage",
    "reporters": ["default", "jest-junit"]
  }
}
